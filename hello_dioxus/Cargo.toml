[package]
name = "hello_dioxus"
version = "0.1.0"
edition = "2021"

[dependencies]
dioxus = { version = "0.6", default-features = false, features = ["minimal", "web"] }

[profile]

[profile.wasm-dev]
inherits = "dev"
opt-level = 1

[profile.server-dev]
inherits = "dev"

[profile.android-dev]
inherits = "dev"
